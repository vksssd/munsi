<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ripple_color"> <!-- Replace with your desired ripple color -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Replace with the background color of your item -->
            <corners android:radius="8dp" /> <!-- Optional: Add rounded corners -->
        </shape>
    </item>
</ripple>